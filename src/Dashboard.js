import React from "react";

function Dashboard() {
  return (
    <div style={{ textAlign: "center", marginTop: "50px" }}>
      <h2>Dashboard</h2>
      <p>This is where we will add admin login & CRUD later.</p>
    </div>
  );
}

export default Dashboard;
